# Card Tap Core

The Card Tap Core is a variant of the Core that starts a timer every time a valid user ID is tapped on the device, and maintains the session for a pre-defined duration. Tapping again while a session is ongoing will refresh the timer. This allows users to activate equipment without having to unhand their ID. 